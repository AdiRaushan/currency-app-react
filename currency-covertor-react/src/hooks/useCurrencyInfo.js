import { useEffect, useState } from "react";

/* Side effects are things your component does besides rendering, like fetching data, subscribing to events, or updating the DOM directly. */

const useCurrencyInfo = (currency) => {
  return <div></div>;
};

export default useCurrencyInfo;
